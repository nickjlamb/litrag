# Orchestration
langchain
langchain-community
langchain-anthropic
langchain-huggingface

# Embeddings + vector store (local, no managed key)
sentence-transformers
faiss-cpu

# Live corpus ingestion — client for the PubCrawl MCP server
# (server install: npm install -g @pharmatools/pubcrawl)
mcp

# LLM client + eval helpers
anthropic
rapidfuzz
pandas
python-dotenv

# Dev
pytest
