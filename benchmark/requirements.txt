# Benchmark-only dependencies — install in a SEPARATE venv from the repo root's:
#   python -m venv benchmark/.venv && source benchmark/.venv/bin/activate
#   pip install -r benchmark/requirements.txt
#
# ragas 0.4.x imports modules removed in langchain-community 0.4 (langchain 1.x),
# which the main repo uses — hence the pins and the separate venv.
ragas==0.4.3
deepeval==4.2.0
langchain==0.3.27
langchain-community==0.3.31
# anthropic SDK 1.x removed the `temperature` kwarg; ragas' and deepeval's
# released versions still pass it internally, so the benchmark pins the last 0.x.
# (litrag's own judge handles both SDK generations.)
anthropic==0.125.0
rapidfuzz
python-dotenv
